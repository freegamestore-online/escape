{
  "name": "escape",
  "private": true,
  "packageManager": "pnpm@10.30.3",
  "engines": { "node": ">=22" },
  "pnpm": { "onlyBuiltDependencies": ["esbuild"] },
  "scripts": {
    "dev": "pnpm --filter @escape/web dev",
    "build": "pnpm --filter @escape/web build",
    "preview": "pnpm --filter @escape/web preview",
    "typecheck": "pnpm --filter @escape/web exec tsc -b",
    "test": "pnpm --filter @escape/web test"
  }
}
